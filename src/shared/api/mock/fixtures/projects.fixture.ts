import type { ProjectResponse } from '../../contracts/project.contracts'

export const projectMockActorFixture = Object.freeze({
  userId: 12,
  name: '박서은',
  email: 'seoeun.park@example.com',
})

export const projectFixtures: readonly ProjectResponse[] = Object.freeze([])
